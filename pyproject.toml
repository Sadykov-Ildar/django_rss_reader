[project]
name = "django-rss-reader"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.6",
    "docutils>=0.22",  # for admindocs
]
[dependency-groups]
dev = [
    "django-debug-toolbar>=6.0.0",
    {include-group = "test"},
]
test = [
    "coverage>=7.10.6",
]